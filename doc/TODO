* .bashrc (?) -> redirect to zsh, if present
* i3 config
* urxvt
* tmux config
* improve vim indentation
* dunst
* read exclude patterns from file
* automatically clean up backup directory
* zsh, if available (from bash)
* unify the "sub-dotify" handling, much code duplication right now
