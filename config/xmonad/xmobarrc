Config {
	overrideRedirect = False,
	font = "xft:Ubuntu Nerd Font:size:10:antialias=true",
	bgColor = "#353839",
	fgColor = "#faebd7",
	position = Static { xpos = 5, ypos = 0, width=1660, height = 30 },

	sepChar = "%",
	alignSep = "}{",
	template = " %XMonadLog% }{ %date% ",
	lowerOnStart = False,
	hideOnStart = False,
	allDesktops = True,
	persistent = True,

	commands = [
		Run XMonadLog,
		Run Date "%a %b %d, %H:%M %Z  " "date" 30
	]
}
